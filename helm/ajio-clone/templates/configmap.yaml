{{- if .Values.config.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ajio.fullname" . }}-config
data:
  {{- range $k, $v := .Values.config.data }}
  {{ $k }}: |
    {{ $v | nindent 4 }}
  {{- end }}
{{- end }}
